[package]
name = "dev-async"
version = "0.9.5"
edition = "2021"
rust-version = "1.75"
readme = "README.md"
license = "Apache-2.0"

authors = [
    "James Gober <me@jamesgober.com>"
]

description = "Async runtime verification for Rust: timeouts, deadlock detection, task tracking, hung-shutdown probes. The async failure modes that synchronous tests miss. Part of the dev-* verification collection."

keywords = [
    "async",
    "tokio",
    "deadlock",
    "timeout",
    "shutdown"
]

categories = [
    "development-tools",
    "development-tools::testing",
    "asynchronous"
]

documentation = "https://docs.rs/dev-async"
repository = "https://github.com/jamesgober/dev-async"
homepage = "https://github.com/jamesgober/dev-async"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
dev-report = { version = "0.9", path = "../dev-report" }
tokio = { version = "1", features = ["rt", "rt-multi-thread", "sync", "time", "macros"] }
pin-project-lite = { version = "0.2", optional = true }

[dev-dependencies]

[features]
default = []
block-detect = ["dep:pin-project-lite"]

[profile.release]
opt-level = 3
lto = "thin"
